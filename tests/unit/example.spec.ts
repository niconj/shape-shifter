import { shallowMount } from '@vue/test-utils';
import ShapeCanvas from '@/components/ShapeCanvas.vue';

describe('ShapeCanvas.vue', () => {
  it('renders props.msg when passed', () => {
    const wrapper = shallowMount(ShapeCanvas, { });
  });
});
